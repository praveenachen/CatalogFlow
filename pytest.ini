[pytest]
testpaths = backend/tests
pythonpath = backend
filterwarnings =
    # Starlette 1.6.0 issue #3497, fixed upstream after the current release.
    ignore:The anyio\.abc\.BlockingPortal alias is deprecated.*:DeprecationWarning:starlette\.testclient
